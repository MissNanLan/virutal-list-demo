name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check run head_ref
        run: echo "The head_ref is ${{ github.head_ref }}"
      - name: Echo OS
        run: echo "The os is ${{ runner.os }}"
